import { NiceDateNamePipe } from './nice-date-name.pipe';

describe('NiceDateNamePipe', () => {
  it('create an instance', () => {
    const pipe = new NiceDateNamePipe();
    expect(pipe).toBeTruthy();
  });
});
